export type onChangeMyPass = {
	e: InputEvent
}

export type errMsg = {
	errText: string,
	errColor: string,
	isEmail: boolean,
	verificationText: string,
	verificationColor: string,
	isVerified: boolean,
	isSubmitButton: boolean,
	myPassword: string
}


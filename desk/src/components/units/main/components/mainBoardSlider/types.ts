export type MainBoardSliderProps = {
  images: string[]
  titles: string[]
  writers: string[]
  writerImages: (string | null | undefined)[]
  boardIds: string[]
  userIds: string[]
  isLikedArray: boolean[]
}
